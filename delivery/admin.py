from django.contrib import admin

# Since PyMongo is being used instead of Django ORM, Django's admin panel won't work.
# You can remove this file or leave it empty.

# Admin panel requires Django ORM models, but you're using MongoDB with PyMongo.
